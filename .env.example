# hive-mcp Environment Configuration
# Copy this file to .env and customize for your setup
#
# Usage: cp .env.example .env && source .env

# =============================================================================
# Required: Repository Paths
# =============================================================================

# Path to bb-mcp repository (lightweight MCP proxy)
BB_MCP_DIR="${HOME}/projects/bb-mcp"

# Path to hive-mcp repository (auto-detected by start-bb-mcp.sh if in same dir)
# HIVE_MCP_DIR="${HOME}/projects/hive-mcp"

# =============================================================================
# Optional: OpenRouter API (for LLM delegation)
# =============================================================================
# Get your key at: https://openrouter.ai/keys
# Free tier models available: devstral-2512, gemma-3, gpt-oss-120b

# OPENROUTER_API_KEY="sk-or-v1-..."

# =============================================================================
# Optional: Ports (defaults shown)
# =============================================================================

# nREPL port for Clojure REPL connection
BB_MCP_NREPL_PORT=7910

# WebSocket channel for hivemind push events
# HIVE_MCP_CHANNEL_WS_PORT=9999

# Chroma vector database
# HIVE_MCP_CHROMA_PORT=8000

# =============================================================================
# Optional: Ollama Configuration (for local LLM/embeddings)
# =============================================================================

# Ollama host (default: http://localhost:11434)
# OLLAMA_HOST="http://localhost:11434"

# Embedding model for semantic search
# HIVE_MCP_EMBEDDING_MODEL="nomic-embed-text:latest"

# =============================================================================
# Optional: Observability (when using --profile observability)
# =============================================================================

# Grafana admin credentials (defaults shown)
# GF_SECURITY_ADMIN_USER=admin
# GF_SECURITY_ADMIN_PASSWORD=hivemcp

# =============================================================================
# Development/Debug
# =============================================================================

# Enable debug logging
# HIVE_MCP_DEBUG=true

# Log file location (default: ~/.config/hive-mcp/server.log)
# HIVE_MCP_LOG_FILE="${HOME}/.config/hive-mcp/server.log"
